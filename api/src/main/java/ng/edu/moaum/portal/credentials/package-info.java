/**
 * Transcripts and certificates (credentials schema, V005 and V013). A
 * transcript is generated from the approved record, verified by Exams and
 * Records and signed by the Registrar — never re-typed, never released
 * while a unit holds the candidate. A certificate is printed only against a
 * Senate-approved, cleared graduand, on stationery whose every serial is
 * accounted for.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Credentials")
package ng.edu.moaum.portal.credentials;
