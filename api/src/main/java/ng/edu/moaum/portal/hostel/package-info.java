/**
 * Hostel and accommodation (V030): the inventory, the session's fee and hold
 * window, the applications, the draw from a published seed, the holds that
 * lapse on their own, the accommodation fee as a payment reference like every
 * other, and the maintenance requests raised from a room. The office is
 * Student Services (and Housing); the student's side reads the same record.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Hostel")
package ng.edu.moaum.portal.hostel;
