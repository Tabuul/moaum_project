/**
 * The applicant's side of admissions (V021, proto/part13.html): Post-UTME
 * registration from the JAMB number on the list the Academic Office loaded,
 * the application account and its sign-in, and the application itself —
 * fee reference, form, documents, screening slip and result, the Board's
 * decision, acceptance, clearance, and the number issued over the register.
 * Everything the applicant sees is what the Academic Office already holds;
 * the screening score is computed under the Office's own settings and the
 * O'Level score is never shown here.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Applicant")
package ng.edu.moaum.portal.applicant;
