/**
 * The CBT question bank (V077): questions authored per course, with the metadata
 * (topic, difficulty, marks) that lets a paper be assembled to a blueprint.
 * Reads {@code catalogue} and {@code assessment} through its own SQL.
 */
@org.springframework.modulith.ApplicationModule(displayName = "CBT")
package ng.edu.moaum.portal.cbt;
