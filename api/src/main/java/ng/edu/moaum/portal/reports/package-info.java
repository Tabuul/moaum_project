/**
 * The University's statutory returns: enrolment by faculty, programme and level,
 * and revenue by category, read straight off the register. A return is a view of
 * the record — read-only, verified against it, never a change to it. The
 * admissions return is drawn from the admissions cycle it already answers with.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Reports")
package ng.edu.moaum.portal.reports;
