/**
 * The NELFUND wallet and the remittances behind it (V033): a remittance
 * loaded and split across named students against the register, suspense
 * that is owned and not parked, an append-only wallet whose balance is
 * derived, applying it to the session charge through the same confirmation
 * every payment passes, a top-up as a reference like every other, and the
 * Fund's decision lists so a student knows where they stand before
 * registration rather than at it.
 */
@org.springframework.modulith.ApplicationModule(displayName = "NELFUND wallet")
package ng.edu.moaum.portal.wallet;
