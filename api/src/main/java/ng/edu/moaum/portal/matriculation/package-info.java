/**
 * Matriculation (people schema, V013 and V016): the faculty list generated
 * from approved registrations and confirmed by the Faculty Officer, the
 * names held under query with a reason, and the run — one transaction over
 * one sequence per department, contiguous, never reused.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Matriculation")
package ng.edu.moaum.portal.matriculation;
