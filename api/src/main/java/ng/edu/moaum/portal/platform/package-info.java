/**
 * The platform: the request path (ARC §7) — correlation id, security,
 * the audit context on every transaction, problem responses — and the
 * service's own status. Nothing here knows a business rule.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Platform")
package ng.edu.moaum.portal.platform;
