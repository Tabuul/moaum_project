/**
 * Graduation (records schema, V013): the degree audit, computed from the
 * published record and never compiled by hand; the classification from the
 * versioned table in force; and the list Senate approves, on its minute.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Graduation")
package ng.edu.moaum.portal.graduation;
