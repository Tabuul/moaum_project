/**
 * The payment gateways: a hosted checkout the applicant is sent to against a
 * fee reference this portal generated, and the gateway's signed webhook that
 * confirms the payment as the Bursary would — an attributed act, the same
 * {@code admissions.confirm_fee}, with the gateway's own reference on the
 * record. Paystack and Flutterwave, each switched on by its secret; while
 * neither is set the button says so and the Bursary confirms by hand.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Payments")
package ng.edu.moaum.portal.payments;
