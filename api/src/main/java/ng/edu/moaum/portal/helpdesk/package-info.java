/**
 * ICT support tickets (V251): a student or a member of staff reports a
 * problem to the Directorate of ICT and receives a tracking number; the
 * desk's agents open, take, work, resolve and close it on the record,
 * with internal notes the requester never sees and updates they do; the
 * requester confirms the resolution or reopens on a reason; the Director
 * of ICT sees everything, keeps the categories and the SLAs, and reads
 * the figures. Every act is on the ticket's own history, written once.
 */
@org.springframework.modulith.ApplicationModule(displayName = "ICT support tickets")
package ng.edu.moaum.portal.helpdesk;
