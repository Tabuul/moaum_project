/**
 * Course spaces (V035): built from the approved registrations. The lecturer
 * publishes material and sets assignments; the students on the roll read and
 * submit; every read is counted; the gradebook total, promoted, lands in the
 * CA column of the score sheet as a new version with its reason.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Course spaces")
package ng.edu.moaum.portal.lms;
